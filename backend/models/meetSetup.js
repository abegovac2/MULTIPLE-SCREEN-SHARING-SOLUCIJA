/*
const Sequelize = require("sequelize");
const sequelize = require("../database/database");

const MeetSetup = sequelize.define("meet", {
    userType: {
        type: Sequelize.STRING,
        field: "userType",
        primaryKey: true,
        unique: true,
        allowNull: false
    },
    setupData: {
        type: Sequelize.STRING,
        field: "setupData",
        unique: true,
        allowNull: false
    }

});

module.exports = MeetSetup;
*/